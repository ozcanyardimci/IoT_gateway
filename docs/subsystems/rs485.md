# RS485 Subsystem — Build Plan

**Status:** Steps 1-3 (front end, protection network, connector) locked and
datasheet-verified. Schematic capture (KiCad) not started — next hands-on step.

## Scope

One isolated half-duplex RS485 bus (A/B differential pair) on IOBOARD, referenced to its
own isolated ground domain (`GND_RS485_ISO`, per the grounding architecture already fixed
for this project — isolated domains stay local to IOBOARD, never tied to `GND_LOGIC`). Not
in scope here: RS232 (separate subsystem, shares the same connector family convention but
different transceiver), PCB layout, exact connector pinout on the panel silkscreen.

## Design approach

Four decisions, in the order they were made:

1. **Transceiver: Mornsun TD5(3)21S485H-A, isolated RS485 module.** Already the standing
   choice for this project (see `hardware/datasheets/README.md`) — an isolated
   transceiver-in-a-module rather than a bare RS485 IC + separate isolator, consistent with
   the isolation approach already used for the analog subsystem. Confirmed against the
   Mornsun datasheet this session: 3kVDC isolation, up to 500kbps, **built-in 47kΩ
   pull-down bias on A/B** — this is what makes external bias resistors unnecessary
   (decision 3 below isn't a shortcut, it's what the module already does internally).
2. **Termination: 120Ω, switchable (jumper/DIP switch), not permanently populated.**
   Standard RS485 practice — only the two physical ends of a multi-drop bus should be
   terminated, and a gateway device doesn't know in advance whether it will sit at an end
   or in the middle of the run. Hard-wiring 120Ω would be wrong in the common case where
   this unit isn't the last device on the bus.
3. **No external bias resistors.** Redundant with the module's internal 47kΩ pull-downs —
   adding external bias resistors on top would just add parts without adding protection
   against the undefined/floating-bus state the internal bias already handles under normal
   conditions.
4. **Protection tier: full network (GDT + series R + TVS + common-mode choke), not
   TVS-only.** This is a general-purpose industrial gateway product, not a single
   fixed-site install with a known-benign cable run — the actual field environment
   (proximity to VFDs/motors/contactors, indoor/outdoor, cable length) is unknown at
   manufacturing time and out of this project's control. Basic TVS-only is the right
   choice only when the installation is known to be short, indoors, and away from
   switching inductive loads; that can't be assumed here. See Step 2 for the full
   component-level reasoning and verified part selection.

## Step 1 results: front end, termination, connector (locked)

| Parameter | Value | Source |
|---|---|---|
| Transceiver | Mornsun TD5(3)21S485H-A | [Mornsun datasheet](https://www.mornsun-power.com/public/uploads/pdf/TD5(3)21S485H-A.pdf) |
| Isolation | 3kVDC (2-port isolation, 1-min withstand) | Mornsun datasheet |
| Max data rate | 500kbps | Mornsun datasheet |
| Internal A/B bias | 47kΩ pull-down, built into the module | Mornsun datasheet |
| Termination | 120Ω, switchable (jumper), not hard-wired | Standard RS485 multi-drop practice |
| External bias resistors | None — redundant with module's internal bias | — |

Connector: locked in Step 3, below.

## Step 2 results: protection network (2026-09-08)

### Reasoning: why the full network, not TVS-only

A basic TVS clamps fast transients but has limited energy-handling capacity and can't
survive a sustained or high-energy surge (induced lightning coupling, a nearby motor/VFD
switching event coupling into a long cable run). A gas discharge tube (GDT) handles the
high-energy front end (survives kA-level 8/20µs pulses) but is too slow and has too high a
follow-on voltage to protect the transceiver by itself. The standard industrial pattern —
and the one this design follows — is a cascade: GDT absorbs the bulk of the energy, a
series resistor limits current into the fast clamp during the GDT's turn-on delay, a TVS
diode array does the fast low-voltage clamp right at the transceiver, and a common-mode
choke suppresses the EMI/common-mode noise a GDT+TVS network doesn't address at all.

This exact cascade (GDT → series R → TVS) matches Bourns' own RS-485 port protection
reference design (design note below), and the GDT part specifically was flagged during
the original hardware teardown as the part identified on the reference RS485 line (see
`hardware/datasheets/README.md`, "Not yet re-verified this session") — re-verified against
its real datasheet this session, not carried over as an assumption.

### Selected components (verified against real datasheets/distributor pages, 2026-09-08)

| Ref | Part | Role | Key specs | Verified against |
|---|---|---|---|---|
| GDT1 | **Bencent B3D090L-C** | Gas discharge tube, first-stage surge diversion (A-GND, B-GND) | 90V DC spark-over, 5kA @ 8/20µs, 3-pole SMD, 1.5pF, -40 to +90°C | [LCSC part page](https://www.lcsc.com/product-detail/Gas-Discharge-Tube-GDT_Bencent-B3D090L-C_C511253.html) — datasheet explicitly lists RS485/232/422 as the intended application |
| R_A/R_B | Generic 27Ω, 0.25W | Series current-limiting into the TVS stage | Negligible IR drop at RS485 signal currents; standard cascade value | Standard practice — not a datasheet-derived value, flagged below |
| D_TVS | **Bourns CDSOT23-SM712** | Fast bidirectional TVS array, A-GND/B-GND, second-stage clamp | Asymmetric −7V/+12V working range (matches RS485 common-mode spec exactly), 19A peak pulse (8/20µs), 75pF, SOT-23-3 | [Littelfuse SM712 datasheet](https://www.littelfuse.com/assetdocs/littelfuse-tvs-diode-array-sm712-datasheet?assetguid=8313a28c-8802-4d47-a2a7-e30b5b1f67d8) (Bourns CDSOT23-SM712 is the same silicon, cross-sourced) |
| L1 | **Würth WE-SL2 744227** | Common-mode choke, EMI/common-mode suppression on the A/B pair | 51µH, 5500Ω impedance @ 100MHz, 1A rated, 0.16Ω DCR | [Würth WE-SL2 product page](https://www.we-online.com/en/components/products/WE-SL2); Würth's own RS-485 EMI filtering app note (ANP083) uses this series on this exact interface |

Signal-integrity check against the 500kbps max data rate (Step 1): the TVS's 75pF and the
choke's 51µH are both far outside anything that would round off a 500kbps edge — the
choke's rated flat-impedance band (100MHz) is roughly 200x the signal's dominant harmonic
content. No margin concern here.

### Still open (flag, not blocking)

- **Series resistor value (27Ω) is standard-practice, not independently derived for this
  specific cascade** — cross-check against the Bencent GDT's typical follow-current and
  the SM712's max peak-pulse rating before schematic lock, same as the "flag confidence
  level" policy used elsewhere in this project (e.g. W5500 max current in the power
  budget).
- **Cascade physical ordering** (field connector → GDT → series R → TVS → choke →
  transceiver) follows the standard industrial pattern and the Bourns reference design's
  sequencing, but hasn't been cross-checked against Würth's own ANP083 app note diagram
  specifically (that PDF 404'd when fetched this session — retry before finalizing layout).

### Datasheet README updates needed

Add to `hardware/datasheets/README.md` under a new "RS485 subsystem" section: Bencent
B3D090L-C (move out of "Not yet re-verified" — now verified), Bourns CDSOT23-SM712, Würth
WE-SL2 744227. Keep the Mornsun TD5(3)21S485H-A entry where it already is.

## Step 3 results: connector (2026-09-08)

**Phoenix Contact MC 1,5/3-ST-3,5** (MPN 1840379) — same family already used for J1
(power input, 1840366), J2 (digital inputs), J3 (relay outputs), and J4 (analog I/O,
1840382): 3.5mm pitch, 28-16AWG (1.5mm²) screw terminals, through-hole pluggable, 3
positions. Confirmed in stock via Newark/Farnell/RS listings.

3 positions, not more: A, B, and one **GND_RS485_ISO** reference pin — matches the
isolated-domain pattern already used for the analog connector (one shared isolated
return, not a per-signal return), and matches what the transceiver module actually needs
(the isolated side only ever sees A, B, and its own isolated ground). Rated 8A/160V, which
is heavy overkill for signal-level current — kept anyway for BOM/part-family consistency
with J1-J4, same reasoning already used project-wide for reusing one connector family
across subsystems.

## Steps

1. **Front end: transceiver, termination, bias** — **DONE (this doc)**.
2. **Protection network: GDT + series R + TVS + common-mode choke** — **DONE (this doc)**,
   component selection verified against real datasheets.
3. **Connector selection** — **DONE (this doc)** — Phoenix Contact MC 1,5/3-ST-3,5.
4. **Schematic capture (KiCad)** — not started. Hands-on step (Ozcan, in KiCad), same
   division of labor as every prior subsystem.
5. **Verification checklist / ERC** — not started, follows schematic capture.
6. **Acceptance criteria** — not started.
7. **Documentation & BOM** (`hardware/bom/rs485_bom.csv`) — not started, follows schematic
   capture per this project's established pattern (BOM finalized once the schematic is
   locked, not before).
8. **Sign-off** — not started.

## Revision history

| Date | Change |
|---|---|
| 2026-09-08 | Step 1 (front end, termination, bias) and Step 2 (protection network) locked. Transceiver (Mornsun TD5(3)21S485H-A), termination (120Ω switchable), and no-external-bias decisions re-verified against the real datasheet this session (confirms module's internal 47kΩ pull-down, 500kbps max, 3kVDC isolation). Protection network selected as full GDT+R+TVS+choke cascade (Bencent B3D090L-C, generic 27Ω, Bourns CDSOT23-SM712, Würth WE-SL2 744227) rather than TVS-only, on the reasoning that this is a general-purpose industrial product with an unknown field environment. GDT choice cross-confirmed against the original hardware teardown's flagged-but-unverified part. |
| 2026-09-08 | Step 3 connector locked: Phoenix Contact MC 1,5/3-ST-3,5 (MPN 1840379), 3 positions (A, B, GND_RS485_ISO), same family as J1-J4. |
