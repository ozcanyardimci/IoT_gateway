# RS485 Subsystem — Build Plan

**Status:** Complete. All 8 plan steps closed, including Step 5 (ERC) — project-wide
ERC confirmed clean 2026-09-23 (updated below; originally deferred to pre-merge, see
`CLAUDE.md` open items for the full history). Both items this doc had carried forward as
open (`RS485_RXD` label direction, U14 connectivity question) are now resolved — see
Step 4 and Step 5 below.

## Scope

One isolated half-duplex RS485 bus (A/B differential pair) on IOBOARD, referenced to its
own isolated ground domain (`GND_RS485_ISO`, per the grounding architecture already fixed
for this project — isolated domains stay local to IOBOARD, never tied to `GND_LOGIC`). Not
in scope here: RS232 (separate subsystem, shares the same connector family convention but
different transceiver), PCB layout, exact connector pinout on the panel silkscreen.

## Design approach

Four decisions, in the order they were made:

1. **Transceiver: Mornsun TD321S485H-A (3.3V variant of the TD5(3)21S485H-A series),
   isolated RS485 module.** Already the standing choice for this project (see
   `hardware/datasheets/README.md`) — an isolated transceiver-in-a-module rather than a
   bare RS485 IC + separate isolator, consistent with the isolation approach already used
   for the analog subsystem. Full datasheet obtained and confirmed 2026-09-08: 3kVDC
   isolation, up to 500kbps, built-in 47kΩ pull-down bias on A/B, difference input
   impedance valid over -7V to +12V common-mode range, exact 10-pin pinout (below).
2. **No on-board termination network.** Follows the transceiver's reference circuit
   exactly — termination isn't shown in the datasheet's application circuits because it's a
   bus-topology property (does this unit sit at the end of a multi-drop run, or in the
   middle), not a transceiver characteristic, so a module datasheet has no fixed answer to
   give either way. Decision: no 120Ω termination network on this board; handled off-board
   per installation if a given deployment needs it.
3. **External bias resistors: yes, included.** ~~Originally decided against these,
   reasoning the internal 47kΩ pull-down was redundant with them.~~ **Correction
   (2026-09-08):** Mornsun's own datasheet ships a second reference circuit, "Fig. 2: Port
   protection circuit for harsh environments," which explicitly adds external Rpullup
   (from VO/pin7) and Rpulldown (from RGND/pin10) on top of the internal bias — and that
   figure is Mornsun's answer to exactly our situation (industrial, not benign). Dropping
   external bias was inconsistent with also choosing the full harsh-environment protection
   tier in decision 4. See Step 2.
4. **Protection tier: full network (GDT + series R + TVS + common-mode choke), not
   TVS-only.** This is a general-purpose industrial gateway product, not a single
   fixed-site install with a known-benign cable run — the actual field environment
   (proximity to VFDs/motors/contactors, indoor/outdoor, cable length) is unknown at
   manufacturing time and out of this project's control. Basic TVS-only is the right
   choice only when the installation is known to be short, indoors, and away from
   switching inductive loads; that can't be assumed here. As of 2026-09-08 this network is
   no longer a generic industry-practice cascade — it's Mornsun's own published
   harsh-environment reference circuit for this exact transceiver. See Step 2.
5. **The 3rd connector position is `EARTH`, not `GND_RS485_ISO`.** Corrected after a
   pixel-level re-check of Fig. 2 (an earlier pass at this same figure misread the
   junction). GDT's third electrode, the far side of the R3/C1 snubber, and the field
   connector's 3rd terminal are all one node in Mornsun's own circuit — that node is what
   the datasheet labels `EARTH`. RGND (pin 10) never reaches the connector at all in their
   reference; it's used only locally, to feed Rpulldown. Reasoning for why this still
   safely coexists with the isolation barrier: the diode-common node (D1/D2 cathodes) sits
   on the *isolated* side of R3/C1, so R3 (1MΩ) keeps DC leakage between the isolated
   domain and earth negligible in normal operation, while C1 gives a low-impedance path
   during a fast surge — R3/C1 is deliberately the boundary between "isolated" and "earth,"
   not a plain short between them. This also matches the datasheet's own precaution to
   ground the cable shield and use a single-point earth connection per network. Connector
   stays 3-position: A, B, `EARTH`. `GND_RS485_ISO` remains a real net on this sheet (RGND,
   Rpulldown) — it just doesn't go to the connector.

## Step 1 results: front end, termination, connector (locked)

| Parameter | Value | Source |
|---|---|---|
| Transceiver | Mornsun TD321S485H-A (TD5(3)21S485H-A series, 3.3V variant) | [Mornsun datasheet](https://www.mornsun-power.com/public/uploads/pdf/TD5(3)21S485H-A.pdf), full PDF obtained 2026-09-08 |
| Isolation | 3kVDC (2-port isolation, 1-min withstand, leakage <1mA); 1000MΩ insulation resistance @500VDC | Mornsun datasheet |
| Max data rate | 500kbps | Mornsun datasheet |
| Internal A/B bias | 47kΩ pull-down, built into the module | Mornsun datasheet |
| Difference input impedance | ≥96kΩ, valid over -7V ≤ Vcm ≤ +12V | Mornsun datasheet |
| Termination | None on-board — matches reference circuit; off-board if a deployment needs it | Termination is a bus-topology property, not a transceiver spec — not present in the datasheet's application circuits |
| External bias resistors | Rpullup (VO/pin7 → A-side node), Rpulldown (RGND/pin10 → B-side node) — see Step 2 | Mornsun Fig. 2 (corrects earlier "none" decision) |

**Full pinout** (confirmed against datasheet page 5, "Pin-Out" table):

| Pin | Mark | Function | Side |
|---|---|---|---|
| 1 | VCC | Input Power+ | Logic |
| 2 | GND | GND | Logic |
| 3 | TXD | Send pin | Logic |
| 4 | RXD | Receiving pin | Logic |
| 5, 6 | — | Not present (isolation gap — package skips these positions) | — |
| 7 | VO | Isolation power output, +5V (external pull-up/pull-down only, max 25mA) | Isolated |
| 8 | B | RS485 B line | Isolated |
| 9 | A | RS485 A line | Isolated |
| 10 | RGND | Isolation power output return (external pull-down reference) | Isolated |

Connector: locked in Step 3, below.

## Step 2 results: protection network (2026-09-08, corrected same day)

### Reasoning: why the full network, not TVS-only

A basic TVS clamps fast transients but has limited energy-handling capacity and can't
survive a sustained or high-energy surge (induced lightning coupling, a nearby motor/VFD
switching event coupling into a long cable run). A gas discharge tube (GDT) handles the
high-energy front end (survives kA-level 8/20µs pulses) but is too slow and has too high a
follow-on voltage to protect the transceiver by itself. The standard pattern is a cascade:
GDT absorbs the bulk of the energy, series resistors limit current into the fast clamp
during the GDT's turn-on delay, TVS diodes do the fast low-voltage clamp right at the
transceiver, and a common-mode choke suppresses the EMI/common-mode noise a GDT+TVS
network doesn't address at all.

### Correction: this is now Mornsun's own reference circuit, not a generic cascade

The first pass at this step (component table below, superseded) was built from a generic
Bourns RS-485 port-protection evaluation-board note, because the transceiver's own
datasheet couldn't be read for a pin diagram (see the schematic-capture blocker below).
Once the full Mornsun datasheet was obtained, it turned out to contain its own "Fig. 2:
Port protection circuit for harsh environments" — literally labeled for our exact
situation — with named parts and values. That supersedes the generic cascade: a
manufacturer's own tested reference circuit for this specific transceiver beats a good
generic industry pattern.

### Selected components — Mornsun's own harsh-environment reference (Fig. 2), 2026-09-08

| Ref (Mornsun's Fig. 2) | Part | Role | Key specs | Verified against |
|---|---|---|---|---|
| GDT | **Bencent B3D090L-C** (Mornsun specifies base part "B3D090L"; -C is Bencent's SMD case variant) | Gas discharge tube, 3-electrode: two line pins wired A-B (line-to-line), third (common) electrode bonds to `EARTH` for line-to-earth diversion — see decision 5 | 90V DC spark-over, 5kA @ 8/20µs, 3-pole SMD, 1.5pF, -40 to +90°C | Mornsun datasheet p.4 names "B3D090L" directly; [LCSC part page](https://www.lcsc.com/product-detail/Gas-Discharge-Tube-GDT_Bencent-B3D090L-C_C511253.html) confirms RS485/232/422 application — same part independently flagged during the original hardware teardown |
| R1, R2 | Generic 2.7Ω, 2W | Series current-limiting, field side of the choke | Mornsun-specified value/power rating | Mornsun datasheet p.4, "Recommended components and values" table |
| R3 | Generic 1MΩ | Bleed resistor, paired with C1 in an RC snubber to EARTH | Mornsun-specified | Mornsun datasheet p.4 |
| C1 | Generic 1nF, 2kV | RC snubber to EARTH (with R3) | Mornsun-specified, 2kV rating for surge withstand | Mornsun datasheet p.4 |
| D1, D2, D3 | **Littelfuse SMBJ6.5CA** (x3) | Bidirectional TVS diodes, line-to-ground clamp | 600W, 2-pin DO-214AA (SMB), 6.5V standoff | [Littelfuse SMBJ series](https://www.littelfuse.com/assetdocs/tvs-diodes-smbj-series-datasheet) · [DigiKey](https://www.digikey.com/en/products/detail/littelfuse-inc/SMBJ6-5CA/285958) — same SMBJ family already used in this project for analog-input protection (SMBJ15CA), just the RS485-appropriate voltage |
| T1 | **TDK ACM2520-301-2P** | Common-mode choke, between the transceiver's A/B/bias node and the protection stage | 2520 (metric 2012) SMD common-mode filter, signal-line rated | Mornsun datasheet p.4; [LCSC part page](https://www.lcsc.com/product-detail/Common-Mode-Filters_TDK-ACM2520-301-2P-T002_C76577.html), [TDK product page](https://product.tdk.com/en/search/emc/emc/cmf_cmc/info?part_no=ACM2520-301-2P-T002) |
| Rpullup, Rpulldown | Not value-specified by Mornsun | External bias: Rpullup from VO (pin 7) to the A-side node; Rpulldown from RGND (pin 10) to the B-side node, both ahead of T1 | Mornsun states only a ceiling: isolated VO/RGND pins support external pull-up/pull-down with recommended max current <25mA | Mornsun datasheet p.2 (Output Specifications note) — exact resistor value still open, see below |

**Superseded (2026-09-08, same day — kept for traceability):** Bourns CDSOT23-SM712 (TVS)
and Würth WE-SL2 744227 (common-mode choke), originally selected from a generic Bourns
RS-485 eval-board note before the Mornsun datasheet was in hand. Both were real,
correctly-targeted parts — replaced only because Mornsun's own tested circuit for this
transceiver is a stronger source than a generic reference design.

Signal-integrity check against the 500kbps max data rate (Step 1): none of the newly
selected parts are datasheet-specified with a capacitance/inductance figure precise enough
to redo the earlier margin math, but a signal-line-rated common-mode choke (TDK's own
ACM2520 series, sized for this class of interface) and small-package TVS diodes are, if
anything, a better signal-integrity fit than the higher-capacitance generic parts they
replaced. No new concern raised.

### Still open (flag, not blocking)

- ~~**Rpullup/Rpulldown exact resistor value.**~~ **Resolved 2026-09-10.** Picked 4.7kΩ for
  both (R_pullup1, R_pulldown1), confirmed on the locked schematic. Worst-case current from
  VO (≈5V) through 4.7kΩ is ≈1.1mA — comfortably inside Mornsun's <25mA ceiling on VO/RGND,
  and in the typical RS485 bias range (560Ω-4.7kΩ).
- ~~**Bias polarity double-check.**~~ **Resolved 2026-09-10**, pixel-level re-check of Fig.
  2 (an earlier pass at this same figure had it backwards): Rpullup (from VO) hops over the
  B line and lands on **A** with a filled junction dot; Rpulldown (from RGND) hops over the
  A line and lands on **B**. Confirmed against the schematic's actual wiring (R_pullup1 →
  VO/A, R_pulldown1 → RGND/B) — matches Fig. 2 exactly, no change needed on the sheet.
- **B3D090L vs B3D090L-C.** Mornsun names the base part "B3D090L"; Bencent's orderable SMD
  part is "B3D090L-C". Treating these as the same device (the suffix denoting Bencent's own
  case/lead variant) — reasonable but not independently confirmed against a Bencent
  part-numbering guide.

### Datasheet README updates needed

Add to `hardware/datasheets/README.md` RS485 subsystem section: Littelfuse SMBJ6.5CA and
TDK ACM2520-301-2P (replacing the CDSOT23-SM712/WE-SL2 rows, which move to a superseded
note). Keep Bencent B3D090L-C and Mornsun TD321S485H-A entries.

## Step 3 results: connector (2026-09-08)

**Phoenix Contact MC 1,5/3-ST-3,5** (MPN 1840379) — same family already used for J1
(power input, 1840366), J2 (digital inputs), J3 (relay outputs), and J4 (analog I/O,
1840382): 3.5mm pitch, 28-16AWG (1.5mm²) screw terminals, through-hole pluggable, 3
positions. Confirmed in stock via Newark/Farnell/RS listings.

3 positions: A, B, and **EARTH** (corrected in decision 5 — matches Mornsun's Fig. 2
exactly: GDT's third electrode, the R3/C1 snubber, and the field connector's 3rd terminal
are one node in their own reference circuit, used for the shielded cable's earth/drain
connection). `GND_RS485_ISO` (RGND) stays on this sheet feeding only the bias resistor —
it does not go to the connector. Rated 8A/160V, which is heavy overkill for signal-level
current — kept anyway for BOM/part-family consistency with J1-J4, same reasoning already
used project-wide for reusing one connector family across subsystems.

## Step 4 results: schematic capture (2026-09-10)

**Update 2026-09-23:** the active copy of this sheet is now
`hardware/kicad/ioboard+lteboard/ioboard+lteboard/rs485.kicad_sch`, following the
2026-09-19 board merge (`docs/board-merge.md`) — `ioboard/ioboard/rs485.kicad_sch` below
is frozen, historical only. `RS485_TXD`/`RS485_RXD` were promoted from unwired
hierarchical labels to real MCU connections at roadmap step 6: both now reach
`core-compute.kicad_sch` — TXD on GPIO1, RXD on GPIO2 — crossing the board-to-board
`J_SIG` connector (pins 13/14, since this sheet is IOBOARD-side and `core-compute` is
LTEBOARD-side). Full pinout: `docs/architecture.md`.

Hands-on step, done by Ozcan in KiCad (`hardware/kicad/ioboard/ioboard/rs485.kicad_sch`).
Verified component-by-component against Mornsun Fig. 2 and against the pinout table in
Step 1: all 15 components present (U15 transceiver, U14 choke, R_pullup1/R_pulldown1,
R35/R34, D15/D18/D19, GD1, R36, C23, J5), R_term/SW_term correctly absent per decision 2,
and the EARTH/GDT/connector network matches Fig. 2 exactly (see decision 5). Bias resistor
line assignment (R_pullup1→VO/A, R_pulldown1→RGND/B) also confirmed correct against Fig. 2
— see the resolved item under Step 2 above.

Labels: `3V3_LOGIC` (hierarchical, input) and `GND_LOGIC` (global, input) on U15 pins
1/2 — consistent with project convention (global for the shared logic ground, hierarchical
for everything else). `RS485_TXD`/`RS485_RXD` (hierarchical) on pins 3/4, added on Ozcan's
own initiative since both will cross to a future MCU/host sheet. `GND_RS485_ISO` and
`EARTH` as plain local labels (no direction attribute), matching the project's precedent
for same-sheet, multi-touchpoint nets (`GND_FIELD_DI`, `RELAY1_CTRL`, `DI1_IN`).

**Resolved 2026-09-19 (was open):** `RS485_RXD`'s hierarchical label shape — flagged here
as "input" when it should be "output" (RXD carries data from this sheet to the MCU) — was
corrected during the 2026-09-19 project-wide label-direction audit. Verified 2026-09-23
directly against the live schematic: `RS485_RXD` is `output` on this sheet and `input` on
`core-compute.kicad_sch`, `RS485_TXD` the complementary `input`/`output` pair — consistent
on both ends, no defect.

## Step 5: verification checklist / ERC — DONE, clean (2026-09-23)

Originally deferred to pre-merge (decision, 2026-09-10), matching the exception path this
project used for digital-inputs and relay-outputs — see `CLAUDE.md` open items for the
full history. The deferred project-wide pass happened 2026-09-22/23, covering this sheet
along with every other subsystem: **project-wide ERC confirmed clean.**
`GND_RS485_ISO`'s isolated-ground net was checked specifically (by analogy with other
isolated grounds needing a `PWR_FLAG`) and needs none — no `Power input`-typed pin sits on
this net, so ERC's undriven-input check never fires here; nothing to add.

The one specific finding carried forward from manual (non-ERC) verification — U14
(common-mode choke) pin 2 appearing unconnected, pin 3 appearing shorted onto pin 4's node
— is resolved by the clean ERC result itself: if that connectivity were actually wrong,
ERC would have flagged it. Ozcan's original read (wiring correct as drawn) stands,
confirmed rather than just asserted.

## Step 6 results: acceptance criteria (2026-09-10)

1. Logic-side/bus-side isolation holds at Mornsun's rated 3kVDC (1-minute withstand,
   <1mA leakage), and `GND_RS485_ISO` has no DC path to `GND_LOGIC` or chassis `EARTH`
   except through R36's deliberately high impedance (1MΩ) — datasheet/design-rated, hipot
   verification is a commissioning item (see below), not something checked on paper beyond
   the manufacturer's own spec.
2. With no driver active, the bus sits in a valid fail-safe idle state (|A-B| ≥ 200mV) —
   provided by the internal 47kΩ pull-down plus the external 4.7kΩ Rpullup/Rpulldown pair,
   both within Mornsun's <25mA ceiling on VO/RGND (see the resolved item under Step 2).
3. A surge event at the connector up to GD1's rated 5kA @ 8/20µs is diverted to `EARTH`
   without exceeding any downstream part's rating: GD1 fires first, R34/R35 (2.7Ω) limit
   let-through current into the TVS stage, D15/D18/D19 (SMBJ6.5CA, 6.5V standoff) clamp
   before the transceiver's port sees a damaging voltage.
4. U15's logic side operates correctly from `3V3_LOGIC` (3.15-3.45V per datasheet) and
   draws current within the budget already accounted for in `power.md`'s 3.3V-LOGIC rail
   sizing (37mA typical / 90mA max — no change needed there).
5. Communication is reliable at the full 500kbps rated data rate over the cable
   length/loading an actual deployment uses — bench item, can't be closed on paper.

Items 1 and 5 require real hardware to close out fully — Rev-A bring-up, listed under
Commissioning below. Item 3 (surge survival) is design-margin-verified against datasheet
figures, same as the power subsystem's approach to its own surge item, not independently
re-simulated (no ngspice model built for this sheet, unlike power's inrush simulation).

### Commissioning test items (Rev-A bring-up)

| Item | What to check | Why not closed now |
|---|---|---|
| Isolation barrier | Hipot/isolation check across U15's logic/bus barrier | Isolation rating is a datasheet spec, not independently re-tested on paper |
| Bus communication at 500kbps | Bench test at rated data rate over the actual cable run | Cable length/loading unknown until a real deployment exists |
| Surge survival | Confirm GD1/TVS stage clamps as expected under an actual induced-surge test | Design-margin-verified against datasheet figures only, not bench-tested |

## Step 7 results: documentation & BOM (2026-09-10)

BOM written to `hardware/bom/rs485_bom.csv` (13 lines, same `Ref,Part,Role,MPN,Manufacturer,
Distributor link` format as the other subsystem BOMs), reference designators matched
against the locked schematic. `hardware/datasheets/README.md`'s RS485 section updated to
match: connector role corrected to A/B/EARTH (was still showing the superseded
`GND_RS485_ISO` 3rd position), TVS/resistor/capacitor rows re-labeled with this project's
actual reference designators (D15/D18/D19, R34/R35/R36, C23) alongside Mornsun's Fig. 2
labels for traceability, and the Rpullup/Rpulldown row updated from "still open" to the
locked 4.7kΩ value with its margin check.

## Step 8: sign-off (2026-09-10)

All 8 plan steps closed. Front end, protection network, and connector selection are
datasheet-verified against Mornsun's own Fig. 2 reference circuit for this exact
transceiver (not a generic cascade). Schematic capture is complete and matches that
reference circuit component-for-component. Two items carried forward rather than closed
here, both non-blocking:

- ERC not run this round (Step 5) — deferred to pre-merge per `CLAUDE.md` open items,
  including the U14 pin 2/3 connectivity question for that check to settle.
- `RS485_RXD` hierarchical label shape (input → output) — cosmetic/correctness fix, low
  priority until the host/MCU sheet exists to test against (Step 4 note).

Neither blocks moving on to the next subsystem in the roadmap order (RS232).

## Steps

1. **Front end: transceiver, termination, bias** — **DONE (this doc)**. Corrected same day
   to include external bias resistors once Mornsun's harsh-environment reference circuit
   was obtained.
2. **Protection network: GDT + series R + TVS + common-mode choke** — **DONE (this doc)**,
   now Mornsun's own reference circuit for this transceiver (Fig. 2), not a generic
   cascade — see Step 2's correction note.
3. **Connector selection** — **DONE (this doc)** — Phoenix Contact MC 1,5/3-ST-3,5.
4. **Schematic capture (KiCad)** — **DONE**. Verified against Fig. 2 and the Step 1
   pinout; one small open item (RS485_RXD label direction), see Step 4 results.
5. **Verification checklist / ERC** — **DONE, clean (2026-09-23)** — see Step 5.
6. **Acceptance criteria** — **DONE (this doc)**.
7. **Documentation & BOM** (`hardware/bom/rs485_bom.csv`) — **DONE**.
8. **Sign-off** — **DONE (this doc)**.

## Revision history

| Date | Change |
|---|---|
| 2026-09-08 | Step 1 (front end, termination, bias) and Step 2 (protection network) locked, first pass. Transceiver (Mornsun TD5(3)21S485H-A), termination (120Ω switchable), and no-external-bias decisions verified against the datasheet text available at the time (internal 47kΩ pull-down, 500kbps max, 3kVDC isolation — but pin diagram unreadable, blocking schematic capture). Protection network selected as a generic GDT+R+TVS+choke cascade (Bencent B3D090L-C, generic 27Ω, Bourns CDSOT23-SM712, Würth WE-SL2 744227) modeled on a Bourns RS-485 eval-board note, on the reasoning that this is a general-purpose industrial product with an unknown field environment. GDT choice cross-confirmed against the original hardware teardown's flagged-but-unverified part. |
| 2026-09-08 | Step 3 connector locked: Phoenix Contact MC 1,5/3-ST-3,5 (MPN 1840379), 3 positions (A, B, GND_RS485_ISO), same family as J1-J4. |
| 2026-09-08 | **Correction, same day.** Full Mornsun datasheet obtained (user supplied the PDF directly), resolving the pin-numbering blocker (confirmed pins 1/2/3/4 = VCC/GND/TXD/RXD, 5/6 absent, 7/8/9/10 = VO/B/A/RGND) and revealing the datasheet's own "Fig. 2: Port protection circuit for harsh environments" — a manufacturer-tested reference circuit for this exact transceiver. Superseded the generic Step 2 cascade with Mornsun's named parts (R1/R2 = 2.7Ω/2W, R3 = 1MΩ, C1 = 1nF/2kV, D1/D2/D3 = Littelfuse SMBJ6.5CA, T1 = TDK ACM2520-301-2P, GDT = Bencent B3D090L-C — GDT choice unchanged, now doubly confirmed). Reversed the Step 1 "no external bias resistors" call — Fig. 2 explicitly adds external Rpullup/Rpulldown for harsh environments, which is our situation. Bourns CDSOT23-SM712 and Würth WE-SL2 744227 kept in the doc as superseded, not deleted, for traceability. |
| 2026-09-10 | Transceiver voltage variant confirmed against the reference design's actual part (3.3V, matching the existing `3V3_LOGIC` rail — no change from Step 1). Removed the switchable 120Ω termination network (R_term/SW_term) — not present in the transceiver's reference circuit, and termination is a bus-topology property rather than a transceiver spec; handled off-board per installation instead. |
| 2026-09-10 | **Correction, same day.** Decision 5 rewritten after a closer re-read of Fig. 2: the 3rd connector position is `EARTH` (GDT third electrode + R3/C1 snubber + connector terminal 3 are one node in Mornsun's own circuit), not `GND_RS485_ISO` as first written. `GND_RS485_ISO`/RGND stays on-sheet, feeding only the bias resistor — it never reaches the connector in the reference circuit. Step 3's connector description updated to match (A, B, EARTH). |
| 2026-09-10 | **Correction, same day.** Rpullup/Rpulldown line assignment was backwards in this doc since Step 1 — a pixel-level re-check of Fig. 2 (dot vs. hop-over crossings) shows Rpullup (VO) hops over B and lands on **A**, Rpulldown (RGND) hops over A and lands on **B** — the opposite of what was previously written. The schematic itself (R_pullup1→VO/A, R_pulldown1→RGND/B) was correct all along; only this doc's description was wrong. Step 1's table and Step 2's component table corrected to match. |
| 2026-09-10 | Steps 4-8 closed: schematic capture confirmed against Fig. 2 (Step 4); Step 5 (ERC) deliberately deferred to pre-merge by decision, not run this round — carries forward the U14 pin 2/3 connectivity question flagged during manual verification, unresolved by ERC either way yet; acceptance criteria written (Step 6); BOM written to `hardware/bom/rs485_bom.csv` and `hardware/datasheets/README.md`'s RS485 section corrected to match the locked schematic — connector role fixed to A/B/EARTH (was still showing superseded `GND_RS485_ISO`), reference designators added alongside Mornsun's Fig. 2 labels (Step 7); sign-off (Step 8). Also resolved the Rpullup/Rpulldown resistor-value open item from Step 2: locked at 4.7kΩ each, confirmed on the schematic, ≈1.1mA worst case against Mornsun's <25mA ceiling. One open item carried forward, not closed: `RS485_RXD` hierarchical label shape should be "output", not "input" — low priority until the host/MCU sheet exists. |
| 2026-09-23 | **Doc recovered and brought current.** This file and `hardware/bom/rs485_bom.csv` existed only on the never-merged `subsystem/rs485` branch — `rs485.kicad_sch` itself made it to `main` earlier (2026-09-10, separate recovery), but this doc and its BOM didn't, and stayed missing through the 2026-09-19 board merge and the 2026-09-22/23 step 6/7/ERC work. Recovered onto `step7-board-interconnect` via `git checkout subsystem/rs485 -- docs/subsystems/rs485.md hardware/bom/rs485_bom.csv`, then updated to match current reality: active schematic path is now `ioboard+lteboard/ioboard+lteboard/rs485.kicad_sch`; `RS485_TXD`/`RS485_RXD` wired to real GPIOs (GPIO1/GPIO2) crossing the board-to-board connector; the `RS485_RXD` label-direction item (open since this doc's original close-out) confirmed fixed as of the 2026-09-19 audit; Step 5 (ERC), deferred since 2026-09-10, is now done and clean — which also resolves the carried-forward U14 connectivity question. No remaining open items on this subsystem. |
