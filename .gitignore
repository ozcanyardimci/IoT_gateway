# KiCad
*.bak
*-bak
*-backups/
_autosave-*
*.kicad_prl
fp-info-cache
*.000
\#auto_saved_files#
*.lck

# PlatformIO
.pio/
.pioenvs/
.piolibdeps/

# Editor / OS
.vscode/*
!.vscode/extensions.json
.DS_Store
Thumbs.db

# Claude Code CLI project state (local only)
.claude/
CLAUDE.md

# Reference-hardware teardown photos — never committed, see CLAUDE.md non-negotiable policy
reference-photos/

# ESP-IDF component manager cache and generated sdkconfig dumps (written by
# every `pio run` under pioarduino/ESP-IDF - not source; managed_components/
# alone is 500MB+ of downloaded dependency trees, regenerable from
# platformio.ini/idf_component.yml, never meant to be committed)
firmware/managed_components/
firmware/sdkconfig.*
